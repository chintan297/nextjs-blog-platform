/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "randomuser.me",
      "images.unsplash.com",
      "i.pravatar.cc",
      "ui-avatars.com",
    ],
  },
};

module.exports = nextConfig;
